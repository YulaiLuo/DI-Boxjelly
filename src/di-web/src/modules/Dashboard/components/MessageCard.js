import React from 'react';
import { Card } from 'antd';

export default function MessageCard() {
    return (
        <Card size="small" extra={null} style={{ height: 100 , width: 250, marginTop:20, marginLeft: 'auto', marginRight: 'auto' }}>
            
            <h1>XXX</h1>
            <p>create new mapping task</p>
        </Card>

    );
}
